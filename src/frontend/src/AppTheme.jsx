import {createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
          main: "#069A8E",
          second: "#60a19b",
          background: "#faf9f6",
          backgroundBox: "#e6e6e6",
          labelText: "#696969"
        }
    }
});

export default theme;